# 🐛 调试版本说明

## 当前版本特点

本版本添加了**非常详细的日志输出**，用于诊断古兰经版本过滤问题。

## 日志输出结构

### 5个关键步骤

每个步骤都有清晰的分隔线和标记：

```
═══════════════════════════════════════════════
🕌 STEP 1: 获取用户选择的语言代码
═══════════════════════════════════════════════
🔄 STEP 2: 从API获取翻译数据
═══════════════════════════════════════════════
📋 STEP 3: 前端显示验证
═══════════════════════════════════════════════
📥 STEP 4: 开始下载古兰经翻译版本
═══════════════════════════════════════════════
💾 STEP 5: 保存用户选择到数据库
═══════════════════════════════════════════════
```

## 如何使用

1. **编译并安装应用**
   ```bash
   ./gradlew assembleDebug
   adb install -r app/build/outputs/apk/debug/app-debug.apk
   ```

2. **清除应用数据**（建议，以模拟新用户）
   ```bash
   adb shell pm clear com.quran.quranaudio.online
   ```

3. **启动 Logcat**
   ```bash
   adb logcat | grep "FragOnboardQuranVersion"
   ```

4. **操作应用**
   - 启动应用
   - 选择任意语言
   - 观察日志输出

5. **分析日志**
   - 检查每个 STEP 的输出
   - 查找 ❌ 错误标记
   - 查找 ⚠️ 警告标记

## 详细指南

请查看 `DIAGNOSTIC_TESTING_GUIDE.md` 获取完整的测试和诊断指南。

## 下一步

根据日志输出找到问题后，即可删除这些详细的日志（或者保留为可选的调试模式）。

---

**重要提示**: 这是调试版本，生产环境请移除详细日志以提高性能。

