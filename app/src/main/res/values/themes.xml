<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="PrayerTimes.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="MyAlertDialogStyle2" parent="Theme.AppCompat.DayNight.Dialog.Alert">


        <!-- Used for the buttons -->
        <item name="colorAccent">@color/onSurface2</item>
        <!-- Used for the title and text -->
        <item name="android:textColorPrimary">@color/onPrimary1</item>

        <!-- Used for the background -->
        <item name="android:background">@color/surfaceColor1</item>
        <item name="android:windowBackground">@drawable/bg_home_aya_of_moment</item>
        <item name="android:colorPrimary">@color/onPrimary1</item>
        <item name="android:colorAccent">@color/onPrimary1</item>

    </style>


    <!-- Quran_Module. -->
    <style name="Theme.QuranApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="colorAccent">@color/colorPrimary</item>

        <item name="android:colorPrimary">@color/colorPrimary</item>
        <item name="android:colorAccent">@color/colorPrimary</item>

        <item name="colorControlNormal">@color/colorIcon</item>

        <item name="android:windowBackground">@color/colorBGPage</item>
        <item name="android:statusBarColor">@color/colorPrimary</item>
        
        <!-- Android 35 status bar fix -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowLightStatusBar">false</item>

        <item name="android:textSize">@dimen/dmnCommonSize</item>
        <item name="android:textColor">@color/colorText</item>
        <item name="android:textColorPrimary">@color/colorText</item>

        <item name="android:clipToPadding">false</item>

        <item name="android:autofillHints" tools:targetApi="o">@null</item>
        <item name="android:importantForAutofill" tools:targetApi="o">no</item>

        <item name="android:dropDownListViewStyle">@style/ListView</item>
        <item name="android:colorEdgeEffect">@android:color/transparent</item>

        <item name="chipGroupStyle">@style/ChipGroupStyle</item>
        <item name="chipStyle">@style/ChipStyle</item>
    </style>


    <style name="Theme.QuranApp.Reader">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>