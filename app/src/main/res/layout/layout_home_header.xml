<?xml version="1.0" encoding="utf-8"?>
<!-- 
    STEP 1: Header Background Layer (Reconstructed)
    - Displays only: Greeting, User Name (conditional), Search Icon, Avatar Icon
    - Background: Rectangle.png with mosque_silhouette.png overlay
    - NO prayer time, countdown, or location on this layer
-->
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/header_container"
    android:layout_width="match_parent"
    android:layout_height="240dp"
    android:clipChildren="false"
    android:clipToPadding="false">

    <!-- Background Layer 1: Green Background (Rectangle.png) -->
    <ImageView
        android:id="@+id/bg_rectangle"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitXY"
        android:src="@drawable/rectangle"
        android:contentDescription="@string/app_name" />

    <!-- Background Layer 2: Mosque Silhouette (mosque_silhouette.png) - NO COMPRESSION -->
    <ImageView
        android:id="@+id/bg_mosque"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="bottom|center_horizontal"
        android:scaleType="fitEnd"
        android:adjustViewBounds="true"
        android:src="@drawable/mosque_silhouette"
        android:alpha="0.3"
        android:contentDescription="@string/app_name" />

    <!-- Foreground Content: Greeting, Name, Search, Avatar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="20dp"
        android:paddingEnd="20dp"
        android:paddingTop="16dp"
        android:layout_gravity="top">

        <!-- Top Row: Greeting + Actions -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <!-- Left Side: Welcome Section -->
            <LinearLayout
                android:id="@+id/welcome_section"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/tv_greeting"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/assalamualaikum"
                    android:textColor="@android:color/white"
                    android:textSize="14sp"
                    android:alpha="0.9"
                    android:fontFamily="sans-serif" />

                <!-- User Name (Conditional - Hidden if not logged in) -->
                <TextView
                    android:id="@+id/tv_user_name"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="4dp"
                    android:text=""
                    android:textColor="@android:color/white"
                    android:textSize="20sp"
                    android:textStyle="bold"
                    android:fontFamily="sans-serif-medium"
                    android:visibility="gone"
                    tools:text="Ahmad Maulana"
                    tools:visibility="visible" />

            </LinearLayout>

            <!-- Right Side: Actions (Search + Avatar) -->
            <LinearLayout
                android:id="@+id/actions_section"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <!-- Search Icon -->
                <ImageView
                    android:id="@+id/btn_search"
                    android:layout_width="44dp"
                    android:layout_height="44dp"
                    android:padding="10dp"
                    android:src="@drawable/dr_icon_search"
                    android:contentDescription="@string/search"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    app:tint="@android:color/white" />

                <!-- User Avatar / Login Button -->
                <androidx.cardview.widget.CardView
                    android:id="@+id/card_avatar"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:layout_marginStart="8dp"
                    app:cardCornerRadius="24dp"
                    app:cardElevation="2dp"
                    android:clickable="true"
                    android:focusable="true"
                    android:foreground="?attr/selectableItemBackground">

                    <FrameLayout
                        android:layout_width="match_parent"
                        android:layout_height="match_parent"
                        android:background="@android:color/white">

                        <!-- Default Icon (Not Logged In) -->
                        <ImageView
                            android:id="@+id/img_avatar_default"
                            android:layout_width="match_parent"
                            android:layout_height="match_parent"
                            android:padding="12dp"
                            android:src="@drawable/dr_icon_user"
                            android:contentDescription="@string/login"
                            app:tint="#9E9E9E" />

                        <!-- User Profile Image (Logged In - Hidden by default) -->
                        <ImageView
                            android:id="@+id/img_avatar_user"
                            android:layout_width="match_parent"
                            android:layout_height="match_parent"
                            android:scaleType="centerCrop"
                            android:visibility="gone"
                            android:contentDescription="@string/profile"
                            tools:src="@tools:sample/avatars"
                            tools:visibility="visible" />

                    </FrameLayout>

                </androidx.cardview.widget.CardView>

            </LinearLayout>

        </LinearLayout>

    </LinearLayout>

</FrameLayout>

